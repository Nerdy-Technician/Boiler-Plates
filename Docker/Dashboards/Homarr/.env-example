# ---------------------------------------------------------------------
# Homarr Dashboard - Environment Example File
# Copy to `.env` and fill in your own values.
# ---------------------------------------------------------------------

# Encryption key used by Homarr to secure sensitive data.
# Generate using: openssl rand -hex 32
SECRET_ENCRYPTION_KEY=changeme_secure_key

# Port mapping (host:container)
HOMARR_PORT=7575

# EXTERNAL URL
HOMARR_URL=dashboard.example.com

# Docker network name
DOCKER_NETWORK=HomeLab

# --- User / Permissions ---
# Host user ID (default: your UID = 1000)
PUID=1000

# Host group ID (default: your GID = 1000)
PGID=1000